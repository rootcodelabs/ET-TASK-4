import { AudioStreamFormat } from "microsoft-cognitiveservices-speech-sdk/distrib/lib/src/sdk/Exports.js";
export interface IAudioDestination {
    id(): string;
    write(buffer: ArrayBuffer): void;
    format: AudioStreamFormat;
    close(cb?: () => void, err?: (error: string) => void): void;
}
