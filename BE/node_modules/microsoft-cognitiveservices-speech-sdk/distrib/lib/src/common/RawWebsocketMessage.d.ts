import { MessageType } from "microsoft-cognitiveservices-speech-sdk/distrib/lib/src/common/ConnectionMessage.js";
export declare class RawWebsocketMessage {
    private privMessageType;
    private privPayload;
    private privId;
    constructor(messageType: MessageType, payload: any, id?: string);
    get messageType(): MessageType;
    get payload(): any;
    get textContent(): string;
    get binaryContent(): ArrayBuffer;
    get id(): string;
}
