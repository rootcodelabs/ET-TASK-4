import { ConnectionEvent } from "microsoft-cognitiveservices-speech-sdk/distrib/lib/src/common/ConnectionEvents.js";
import { ConnectionMessage } from "microsoft-cognitiveservices-speech-sdk/distrib/lib/src/common/ConnectionMessage.js";
import { ConnectionOpenResponse } from "microsoft-cognitiveservices-speech-sdk/distrib/lib/src/common/ConnectionOpenResponse.js";
import { EventSource } from "microsoft-cognitiveservices-speech-sdk/distrib/lib/src/common/EventSource.js";
export declare enum ConnectionState {
    None = 0,
    Connected = 1,
    Connecting = 2,
    Disconnected = 3
}
export interface IConnection {
    id: string;
    state(): ConnectionState;
    open(): Promise<ConnectionOpenResponse>;
    send(message: ConnectionMessage): Promise<void>;
    read(): Promise<ConnectionMessage>;
    events: EventSource<ConnectionEvent>;
    dispose(disposing?: string): Promise<void>;
}
